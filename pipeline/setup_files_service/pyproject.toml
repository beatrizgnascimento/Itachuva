[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "setup-files-service"
version = "0.1.0"
description = "Default variables processing"
requires-python = ">=3.10"